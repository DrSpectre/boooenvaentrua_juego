using UnityEngine;

public class MuertePersonaje: MonoBehaviour, ProtocoloMuerte{
    public void activar() {
        Debug.Log("TENEMOS UN PROBLEMA HUSTON");
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start(){
        
    }

    // Update is called once per frame
    void Update(){
        
    }
}
